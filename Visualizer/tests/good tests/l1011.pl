% l1011.json
% "If each of the 10 digits 0-9 is chosen at random, how many ways can you choose a two-digit code number, repeated digits permitted."	100

digits = [0:9];
code in [ || digits];
#code = 2;
